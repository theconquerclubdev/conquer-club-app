<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Matches getTemporaryDirectory() from path_provider, where
         streaks_tab.dart writes the composited streak image. -->
    <cache-path name="social_share_cache" path="." />
</paths>
